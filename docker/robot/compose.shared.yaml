# 共有バス機: 全モータ 12V、canonical ID 1..9 を /dev/lekiwi で所有する。
services:
  robot:
    volumes:
      - type: bind
        source: ${LEKIWI_DEVICE:-/dev/lekiwi}
        target: ${LEKIWI_DEVICE:-/dev/lekiwi}
        bind:
          create_host_path: false
